
import { Suspense } from "react";
import LoginRedirectPageView from "./_view";

const LoginRedirectPage = () => {


    return <Suspense>
        <LoginRedirectPageView/>
    </Suspense>
}

export default LoginRedirectPage
